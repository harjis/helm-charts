# Default values for folder-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

# This is the docker image version number
applicationVersion: 0.1.1

resources: {}
